apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-app
  namespace: default
spec:
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
        prometheus.io/path: "/metrics"
    # ... existing spec ...